2. You have an array  
Create a function getTypes, which accepts 2 parameters - an array of different data and type name in string. 
The function should filter an array and return new array with data type which you added as the second parameter of function. 
Solve the task with for of or for loop only, using array methods are not allowed. Examples: getTypes(dataArray, 'number'); 
// output: [1, 4] getTypes(dataArray, 'string'); // output: ['str', 'test', 'new one', 'there is', 'etc']


const dataArray = ['str', 'test', 1, 4, true, 'new one', 'there is', 'etc', null, undefined];

