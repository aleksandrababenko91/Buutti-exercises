/*Assignment 2
The following code doesn't work.

Figure out why it doesn't work, and fix it so that it works.
const value = 30;
const multiplier = 1.2;
value = value * multiplier;
console.log(value);
*/

// "const" не должна меняться, поэтому я изменил const - let.
let value = 30;
const multiplier = 1.2;
value = value * multiplier;
console.log(value);

